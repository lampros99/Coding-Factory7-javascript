/**
 * Javascript uses a dynimic policy, no datatypes needed.
 * Variables are cameCase.
 * JS is case-sensitive.
 * Avoid declaring with var.
 * Take care of teh syntac.Spaces between operators/operands
 */

const PI = 3.14
const mathThreads = 150

let counter = 0

counter++

console.log(PI, typeof PI, mathThreads, counter)